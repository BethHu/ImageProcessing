# 理想高通滤波工作流测试清单

## 测试步骤

### 步骤 1: 编译项目
- [ ] 打开 Visual Studio 2010
- [ ] 打开 F:\rsdiplib\code\myAlg\myAlg.sln
- [ ] 点击 Build → Build Solution
- [ ] 确保编译成功（0 个错误）

### 步骤 2: 运行程序
- [ ] 启动编译后的 myAlg.exe
- [ ] 主窗口出现

### 步骤 3: 测试理想高通滤波工作流

#### 3.1 点击"理想高通滤波器"按钮
- [ ] 按钮存在并可以点击
- [ ] 出现"傅里叶变换-图像输入选择"对话框

#### 3.2 傅里叶输入对话框
- [ ] 选择图像类型（例如"生成图像"）
- [ ] 设置图像参数（宽、高等）
- [ ] 点击"下一步"按钮
- [ ] **预期**: 进入频谱显示对话框

#### 3.3 频谱显示对话框
- [ ] 自动计算和显示傅里叶频谱
- [ ] 可以看到频谱预览（幅度、相位等）
- [ ] 点击"下一步"按钮
- [ ] **预期**: 进入理想高通滤波器对话框

#### 3.4 理想高通滤波器对话框 ✓ 关键步骤
- [ ] 对话框标题为"理想高通滤波器"
- [ ] 显示"截止频率"编辑框，默认值为 0.300
- [ ] **在对话框打开时应该已经应用了默认滤波器**
- [ ] 可以修改截止频率值（例如 0.1, 0.2, 0.5）
- [ ] 点击"预览"按钮，应该出现消息"已应用理想高通滤波器"
- [ ] 点击"确定"按钮
- [ ] **预期**: 
  - [ ] 如果频率参数改变，会重新应用滤波器
  - [ ] 返回到主对话框
  - [ ] **不会直接跳过滤波器！**

#### 3.5 反变换对话框
- [ ] 对话框标题为"傅里叶反变换与误差分析"
- [ ] 显示三个按钮："计算反傅里叶"、"计算误差"、"比较显示"
- [ ] 点击"计算反傅里叶"按钮
- [ ] **预期**: 
  - [ ] 出现成功消息 "反变换成功（使用过滤后的数据）"
  - [ ] **不是** "反变换成功" （无括号）
  - [ ] 表示已使用过滤后的 DFT 数据进行反变换
- [ ] 点击"比较显示"查看结果

#### 3.6 最终验证
- [ ] 在输出窗口看到 TRACE 信息：
  ```
  应用理想高通滤波器: 截止频率=0.300, 尺寸=256x256, cutoff=...
  理想高通滤波器已应用成功，过滤后数据大小=65536
  ```
- [ ] 反变换消息明确显示 "（使用过滤后的数据）"

---

## 故障排除

### 问题 1: "理想高通滤波器"按钮不存在
- **解决**: 检查 myAlgDlg.h 中是否有 `OnClickedButtonIdealhighpass()` 声明
- **解决**: 检查 myAlg.rc 中是否定义了按钮资源

### 问题 2: 点击按钮后直接进入反变换对话框，跳过滤波器
- **原因**: ApplyIdealHighPassFilter() 没有被调用
- **解决**: 检查 IdealFilterDlg.cpp 的 OnInitDialog() 是否调用了 ApplyIdealHighPassFilter()
- **解决**: 检查反变换消息，如果显示"反变换成功"而不是"反变换成功（使用过滤后的数据）"，说明没有使用过滤后的数据

### 问题 3: 反变换消息显示"反变换成功"（不使用过滤数据）
- **原因**: m_pFilteredDFT 为 nullptr 或为空
- **解决**: 检查 myAlgDlg.cpp 中是否调用了 `inverseDlg.SetFilteredDFT(&filterDlg.m_filteredDFT)`
- **解决**: 检查 filterDlg.m_filteredDFT 是否非空

### 问题 4: 理想高通滤波器对话框中没有看到"截止频率"编辑框
- **原因**: 资源定义中缺少 IDC_EDIT_CUTOFF 控件
- **解决**: 用 Visual Studio 的资源编辑器打开 myAlg.rc，在 IDD_IDEALFILTERDLG 对话框中添加编辑框
- **解决**: 检查 resource.h 中是否有 `#define IDC_EDIT_CUTOFF 1098`

### 问题 5: 修改截止频率后点击"确定"，消息显示"错误：滤波器未正确应用！"
- **原因**: ApplyIdealHighPassFilter() 失败（可能是数据为空）
- **解决**: 在 Visual Studio 输出窗口查看 TRACE 信息
- **解决**: 检查 CFourierSpectrumDlg 的 DFT 计算是否正确

### 问题 6: 在输出窗口看不到 TRACE 信息
- **解决**: View → Output，选择 Debug 标签
- **解决**: 确保程序以 Debug 模式编译和运行
- **解决**: 如果仍然看不到，检查代码中是否正确包含了 TRACE 语句

---

## 成功标志

✓ 当用户看到这个消息流时，表示工作流正确：
```
1. 打开"傅里叶变换-图像输入选择"
2. 打开"傅里叶谱显示设置"（自动计算DFT）
3. 打开"理想高通滤波器"（自动应用默认滤波器）
4. 打开"傅里叶反变换与误差分析"
5. 点击"计算反傅里叶"后显示: "反变换成功（使用过滤后的数据）"
```

✓ 输出窗口显示：
```
应用理想高通滤波器: 截止频率=..., 尺寸=...
理想高通滤波器已应用成功，过滤后数据大小=...
```

✓ 不会出现的情况：
- ✗ 直接从频谱对话框跳到反变换对话框
- ✗ 反变换消息显示"反变换成功"（无括号内容）
- ✗ 理想高通滤波器对话框打开后立即关闭

---

## 有用的快捷方式

### 添加调试输出
在 IdealFilterDlg.cpp 中已有 TRACE 语句，运行时可以在输出窗口看到：
```
TRACE(_T("应用理想高通滤波器...\n"));
```

### 快速测试
1. 编译项目
2. F5 启动调试
3. 点击"理想高通滤波器"
4. 在每个对话框中选择默认选项，点击"下一步"或"确定"
5. 观察消息和输出窗口

