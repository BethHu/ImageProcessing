# 理想高通滤波器使用指南

## 工作流说明

当用户点击"理想高通滤波器"按钮后，应该看到以下**四个连续的对话框**：

### 步骤 1️⃣: 傅里叶输入对话框
**对话框名称**: "傅里叶变换-图像输入选择"

- 选择图像类型（生成或读取）
- 设置图像参数（宽、高等）
- 点击 **"下一步"** 按钮进入下一步

### 步骤 2️⃣: 频谱显示对话框
**对话框名称**: "傅里叶谱显示设置"

- 自动计算傅里叶变换（DFT）
- 显示频谱（幅度、相位等）
- 可调整显示参数
- 点击 **"下一步"** 按钮进入下一步

### 步骤 3️⃣: **理想高通滤波器对话框** ⭐ 关键步骤
**对话框名称**: "理想高通滤波器"

**这个对话框让用户调整滤波参数：**

- **截止频率编辑框** (IDC_EDIT_CUTOFF)
  - 默认值: 0.300
  - 范围: 通常 0.0 - 1.0
  - 可手动输入或用滑块调整

- **预览按钮** (IDC_BUTTON_PREVIEW)
  - 点击后应用当前参数的滤波器
  - 显示预览消息，确认滤波已应用

- **确定按钮** (IDOK)
  - 确认参数并应用滤波器
  - 如果参数改变，会重新应用滤波
  - 关闭对话框进入下一步

- **取消按钮** (IDCANCEL)
  - 取消整个工作流
  - 返回主窗口

### 步骤 4️⃣: 反变换对话框
**对话框名称**: "傅里叶反变换与误差分析"

- 使用理想高通滤波器处理后的 DFT 数据
- 点击 **"计算反傅里叶"** 进行反变换
- 显示最终的滤波结果图像

---

## 常见问题排查

### ❌ 问题 1: "没有看到理想高通滤波器对话框"

**可能原因:**
1. 程序有 bug，对话框没有正确调用
2. 对话框资源定义有问题
3. 对话框初始化失败

**排查步骤:**
1. 查看输出窗口（Debug 标签），应该看到:
   ```
   CIdealFilterDlg 构造函数已调用
   理想高通滤波器对话框已打开
   ```

2. 如果看到错误信息，说明对话框创建失败
3. 检查 resource.h 中是否定义了 IDD_IDEALFILTERDLG
4. 检查 myAlg.rc 中的对话框资源定义

### ❌ 问题 2: "理想高通滤波器对话框出现但立即关闭"

**可能原因:**
1. 对话框资源定义有语法错误
2. 对话框初始化异常

**排查步骤:**
1. 检查 myAlg.rc 中 IDD_IDEALFILTERDLG 的定义
2. 编译时是否有资源编译警告
3. 手动用资源编辑器打开 myAlg.rc 验证

### ❌ 问题 3: "可以看到理想高通滤波器对话框，但没有截止频率编辑框"

**可能原因:**
1. 对话框资源中缺少 IDC_EDIT_CUTOFF 控件
2. 控件位置可能在对话框外部

**解决方案:**
1. 打开 myAlg.rc 在 Visual Studio 的资源编辑器中
2. 找到 IDD_IDEALFILTERDLG 对话框
3. 确保有一个"编辑框"控件，ID 为 IDC_EDIT_CUTOFF
4. 如果没有，手动添加一个

### ❌ 问题 4: "点击预览或确定后显示错误：滤波器未正确应用"

**可能原因:**
1. CFourierSpectrumDlg 的 DFT 数据为空
2. 图像尺寸为 0
3. ApplyIdealHighPassFilter() 函数有 bug

**排查步骤:**
1. 查看输出窗口，应该看到:
   ```
   应用理想高通滤波器: 截止频率=0.300, 尺寸=256x256
   理想高通滤波器已应用成功，过滤后数据大小=65536
   ```

2. 如果看到错误信息，检查频谱对话框是否正确计算了 DFT
3. 检查图像尺寸是否为 0

### ❌ 问题 5: "反变换显示的不是过滤后的结果"

**可能原因:**
1. SetFilteredDFT() 没有被正确调用
2. 过滤后的 DFT 数据没有正确传递

**排查步骤:**
1. 检查 myAlgDlg.cpp 中是否调用了:
   ```cpp
   inverseDlg.SetFilteredDFT(&filterDlg.m_filteredDFT);
   ```

2. 查看反变换对话框的消息，应该显示:
   ```
   "反变换成功（使用过滤后的数据）"
   ```

3. 如果显示 "反变换成功"（无括号），说明没有使用过滤后的数据

---

## 调试技巧

### 启用输出窗口调试信息

1. **打开输出窗口**: View → Output
2. **选择 Debug 标签**
3. **运行程序并观察输出**

输出应该显示:
```
CIdealFilterDlg 构造函数已调用, pSpectrumDlg = 12345678
理想高通滤波器对话框已打开
应用理想高通滤波器: 截止频率=0.300, 尺寸=256x256, cutoff=38.400000
理想高通滤波器已应用成功，过滤后数据大小=65536
```

### 添加断点调试

1. 在 myAlgDlg.cpp 的 OnClickedButtonIdealhighpass() 函数中设置断点
2. 逐步执行，观察每一步的执行情况
3. 检查 filterDlg.DoModal() 的返回值
4. 检查 filterDlg.m_filteredDFT 是否非空

### 资源编辑器验证

1. 在 Visual Studio 中打开 myAlg.rc
2. 在 Dialog 部分找到 IDD_IDEALFILTERDLG
3. 右键选择 "View Designer" 查看对话框布局
4. 确保有以下控件：
   - IDC_EDIT_CUTOFF（编辑框，截止频率）
   - IDC_SLIDER_CUTOFF（滑块，可选）
   - IDC_BUTTON_PREVIEW（按钮，预览）
   - IDOK（按钮，确定）
   - IDCANCEL（按钮，取消）

---

## 预期的用户体验

✅ **正确的流程:**
1. 点击"理想高通滤波器"
2. 输入图像（选择或生成）→ 点击"下一步"
3. 看到频谱预览 → 点击"下一步"
4. **看到理想高通滤波器对话框**
5. 修改截止频率（例如 0.2）
6. 点击"预览"查看效果
7. 点击"确定"应用滤波
8. 在反变换对话框看到过滤后的图像结果

✅ **关键消息:**
- "即将进入理想高通滤波器对话框，请调整参数"
- "已应用理想高通滤波器预览"
- "理想高通滤波器已应用，即将进行反变换"
- "反变换成功（使用过滤后的数据）"

❌ **错误的流程:**
- 跳过理想高通滤波器对话框，直接进入反变换
- 反变换显示"反变换成功"（无括号），说明没有使用过滤数据

